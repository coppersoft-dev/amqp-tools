[loggers]
keys=root

[logger_root]
handlers=terminal
level=WARN

[formatters]
keys=simple

[formatter_simple]
format=%(asctime)s - %(name)s - %(levelname)s - %(message)s

[handlers]
keys=terminal

[handler_terminal]
class=StreamHandler
formatter=simple
args=(sys.stdout,)
